#include "ComparePostions.h"

ComparePositions::ComparePositions(const double* sec_map, const Position* target_pos)
{
	//security_map = sec_map;
	//target = new Cell(target_pos->row, target_pos->col,0,0,0, nullptr);
}

//double ComparePositions::calculateHeuristic(const Position* cur, const Position* target) {
//	double dis = sqrt(pow(cur->row - target->row, 2) + pow(cur->col - target->col, 2));
//	double security = security_map[MSZ * cur->row + cur->col];
//	return dis + security * SECURITY_COEFFICIENT;
//}

